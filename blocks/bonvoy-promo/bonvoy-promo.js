export default async function decorate(block) {
  // The block has a single row with 3 cells (one per column).
  // Each cell has an h4 heading and a ul of links.
  // No DOM restructuring needed — CSS handles the 3-column layout.
}
